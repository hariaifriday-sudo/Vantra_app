// Runtime configuration for the built app. Edit this file directly on the
// deployed machine — no rebuild needed — to point the frontend at a
// reachable backend. Loaded before the app bundle (see index.html).
window.__VANTRA_CONFIG__ = {
  API_BASE: "http://localhost:8000",
};
